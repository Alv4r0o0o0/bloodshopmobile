import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Detailhombre3Page } from './detailhombre3.page';

describe('Detailhombre3Page', () => {
  let component: Detailhombre3Page;
  let fixture: ComponentFixture<Detailhombre3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Detailhombre3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
