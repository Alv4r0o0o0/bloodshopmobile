import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Detailhombre4Page } from './detailhombre4.page';

describe('Detailhombre4Page', () => {
  let component: Detailhombre4Page;
  let fixture: ComponentFixture<Detailhombre4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Detailhombre4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
